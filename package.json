{
  "scripts": {
    "build": "echo 'Static deployment - no build needed'",
    "deploy": "echo 'No deploy script needed - Cloudflare Pages handles this automatically'"
 "dependencies": "next": "^14.2.3" }
}
