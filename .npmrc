# npm configuration for Cloudflare Pages
# 禁用npm clean-install，使用普通install
# 这是因为npm clean-install在某些情况下会出现"Exit handler never called"错误
legacy-peer-deps=true
strict-peer-dependencies=false
engine-strict=false
# 强制使用npm install而不是npm ci
prefer-offline=true
progress=false
audit=false
fund=false